# pRidge_eval

Evaluating the Prior Ridge (pRidge) model against OPR & EPA baselines for FRC data

Functions to function Prior Ridge coefficients and do lambda cross validation can be found in the [scoutR package](https://github.com/GKrotkov/scoutR).
